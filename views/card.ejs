<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Carte fidélité</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    .card { border: 1px solid #ddd; padding: 20px; width: 360px; }
    .barcode { margin-top: 12px; max-width: 100%; }
    .meta { color: #555; margin-top: 6px; }
  </style>
</head>
<body>
  <div class="card" role="main" aria-live="polite">
    <h1>Carte fidélité</h1>

    <p><strong>Prénom :</strong> <%= typeof prenom !== 'undefined' ? prenom : '' %></p>
    <p><strong>Nom :</strong> <%= typeof nom !== 'undefined' ? nom : '' %></p>
    <p class="meta"><strong>Code :</strong> <%= typeof code !== 'undefined' ? code : '' %></p>

    <!-- Exemple d'image barre (si tu as un endpoint /barcode/:code) -->
    <div>
      <img
        class="barcode"
        src="<%= (baseUrl || '') %>/barcode/<%= typeof code !== 'undefined' ? encodeURIComponent(code) : '' %>?t=<%= timestamp %>"
        alt="code-barres"
        onerror="this.style.display='none';"
      />
    </div>

    <p class="meta">Généré le <%= new Date(timestamp).toLocaleString('fr-FR') %></p>
  </div>

  <!-- Eviter JS inline complexe pour ne pas casser le render côté serveur -->
</body>
</html>
