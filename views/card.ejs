<!DOCTYPE html>
<html>
<head>
  <title>Carte Fidélité - <%= carte.prenom %> <%= carte.nom %></title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      background-color: #f5f5f5;
      font-family: Arial, sans-serif;
    }
    .card {
      width: 350px;
      height: 200px;
      background-image: url('/static/carte-mdl.png');
      background-size: cover;
      border-radius: 15px;
      padding: 20px;
      color: #333;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      position: relative;
    }
    .logo {
      text-align: center;
      margin-bottom: 10px;
    }
    .logo img {
      height: 40px;
    }
    .client-info {
      background-color: rgba(255,255,255,0.8);
      padding: 8px 12px;
      border-radius: 8px;
      margin-top: 10px;
      font-size: 14px;
    }
    .barcode {
      margin-top: 15px;
      text-align: center;
    }
    .barcode img {
      height: 40px;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="logo">
      <img src="/static/logo-mdl.png" alt="Logo MDL">
    </div>
    <div class="client-info">
      <p><strong><%= carte.prenom %> <%= carte.nom %></strong></p>
      <p>Code: <%= carte.code %></p>
    </div>
    <div class="barcode">
      <img src="<%= baseUrl %>/barcode/<%= carte.code %>" alt="Code-barres">
    </div>
  </div>
</body>
</html>
