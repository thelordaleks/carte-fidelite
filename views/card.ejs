<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Carte fidélité</title>
  <style>
    /* fond totalement transparent dans l’iframe */
    html, body {
      margin: 0;
      padding: 0;
      background: transparent !important;
      color: #333;
      font-family: Arial, sans-serif;
      overflow: hidden;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
    }

    /* conteneur de la carte : AUCUNE bordure/ombre/padding */
    .card {
      border: none !important;
      padding: 0 !important;
      width: 100%;
      max-width: none;
      background: transparent !important;
      box-shadow: none !important;
      border-radius: 0 !important;
      margin: 0 auto;
    }

    h1 { display: none; } /* on garde le balisage léger mais on n’affiche pas le titre */

    .barcode {
      margin-top: 12px;
      max-width: 100%;
      height: auto;
      border: 0;
      display: block;
    }

    .meta {
      color: #666;
      margin-top: 6px;
      font-size: 13px;
    }
  </style>
</head>
<body>
  <div class="card" role="main" aria-live="polite">
    <h1>Carte fidélité</h1>

    <p><strong>Prénom :</strong> <%= typeof prenom !== 'undefined' ? prenom : '' %></p>
    <p><strong>Nom :</strong> <%= typeof nom !== 'undefined' ? nom : '' %></p>
    <p class="meta"><strong>Code :</strong> <%= typeof code !== 'undefined' ? code : '' %></p>

    <div>
      <img
        class="barcode"
        src="<%= (baseUrl || '') %>/barcode/<%= typeof code !== 'undefined' ? encodeURIComponent(code) : '' %>?t=<%= timestamp %>"
        alt="code-barres"
        onerror="this.style.display='none';"
      />
    </div>

    <p class="meta">Généré le <%= new Date(timestamp).toLocaleString('fr-FR') %></p>
  </div>
</body>
</html>
