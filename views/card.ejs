<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Carte fidélité MDL</title>

  <style>
    html, body {
      margin: 0;
      padding: 0;
      background: transparent !important;
      overflow: hidden;
      text-align: center;
      font-family: Arial, sans-serif;
      color: #333;
    }

    .card {
      background: transparent !important;
      border: none !important;
      box-shadow: none !important;
      width: 100%;
      max-width: 440px;
      margin: 0 auto;
      padding: 0;
    }

    h1 {
      font-size: 22px;
      color: #c0872f;
      margin: 5px 0 8px 0;
      font-weight: bold;
    }

    p {
      margin: 4px 0;
      font-size: 15px;
    }

    .barcode {
      margin-top: 10px;
      max-width: 100%;
      height: auto;
      display: block;
      margin-left: auto;
      margin-right: auto;
      border: none;
      background: transparent !important;
    }

    .meta {
      color: #666;
      margin-top: 8px;
      font-size: 13px;
    }
  </style>
</head>

<body>
  <div class="card" role="main" aria-live="polite">
    <h1>Carte fidélité</h1>

    <p><strong>Prénom :</strong> <%= typeof prenom !== 'undefined' ? prenom : '' %></p>
    <p><strong>Nom :</strong> <%= typeof nom !== 'undefined' ? nom : '' %></p>
    <p class="meta"><strong>Code :</strong> <%= typeof code !== 'undefined' ? code : '' %></p>

    <!-- Code-barres -->
    <div>
      <img
        class="barcode"
        src="<%= (baseUrl || '') %>/barcode/<%= typeof code !== 'undefined' ? encodeURIComponent(code) : '' %>?t=<%= timestamp %>"
        alt="code-barres"
        onerror="this.style.display='none';"
      />
    </div>

    <p class="meta">Généré le <%= new Date(timestamp).toLocaleString('fr-FR') %></p>
  </div>
</body>
</html>
