<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carte fidélité - <%= carte.prenom %> <%= carte.nom %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background-color: #f5f5f5;
    }

    .card-container {
      background-color: white;
      border-radius: 15px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      padding: 30px;
      max-width: 400px;
      width: 100%;
    }

    .logo {
      text-align: center;
      margin-bottom: 20px;
    }

    .logo img {
      max-width: 200px;
      height: auto;
      display: block;
      margin: 0 auto;
    }

    .card {
      background-color: #f8f9fa;
      background-image: url('<%= baseUrl %>/static/carte-mdl.png?t=<%= timestamp %>');
      background-size: cover;
      background-position: center;
      border-radius: 10px;
      padding: 25px;
      margin: 20px 0;
      min-height: 200px;
      position: relative;
      border: 1px dashed #ddd; /* Bordure de débogage */
    }

    .card-content {
      background-color: rgba(255, 255, 255, 0.9);
      padding: 15px;
      border-radius: 8px;
    }

    .barcode {
      text-align: center;
      margin-top: 20px;
    }

    .barcode img {
      max-width: 100%;
      height: auto;
    }

    h2 {
      margin: 0 0 10px 0;
      color: #333;
    }

    p {
      margin: 5px 0;
      color: #555;
    }

    .fallback-image {
      color: #999;
      font-style: italic;
      text-align: center;
      padding: 20px;
    }
  </style>
</head>
<body>
  <div class="card-container">
    <div class="logo">
      <!-- Image avec fallback -->
      <img
        src="<%= baseUrl %>/static/logo-mdl.png?t=<%= timestamp %>"
        alt="Logo MDL"
        onerror="this.onerror=null; this.src='https://via.placeholder.com/200x80/0078D7/FFFFFF?text=MDL';"
      >
    </div>

    <div class="card">
      <div class="card-content">
        <h2><%= carte.prenom %> <%= carte.nom %></h2>
        <p>Code: <%= carte.code %></p>
      </div>
    </div>

    <div class="barcode">
      <img src="<%= baseUrl %>/barcode/<%= carte.code %>?t=<%= timestamp %>" alt="Code-barres">
    </div>
  </div>
</body>
</html>
